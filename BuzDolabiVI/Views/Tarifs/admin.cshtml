@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<BuzDolabiVI.Models.Tarif>



@*<h1>Index</h1>*@
<div id="onaysizTarifler" style="margin-top:100px; text-align:center;">
    <h1 id="">ONAYSIZ TARİFLER İÇİN TIKLAYINIZ.</h1>
</div>
<div id="onayliTarifler" style="margin-top:100px; text-align:center;">
    <h1 id="onayliTarifler">ONAYLI TARİFLER İÇİN TIKLAYINIZ</h1>
</div>
<div class="container" id="onaysiz" style="display:none">
    <div style="margin-top:9%">
        <h2 style="text-align:center;">--- ONAYSIZ --- SON EKLENEN TARİFLER</h2>
        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay == "onay")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İNCELE</a> @item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>
    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYSIZ --- ANA YEMEK TARİFLERİ</h2>

        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay == "onay")
                {
                    continue;
                }
                @if (item.kategori != "anaYemek")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a> @item.goruntulenme görüntülenme
                    </div>
                </li>

            }
        </ul>

    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYSIZ --- ÇORBA TARİFLERİ</h2>

        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay == "onay")
                {
                    continue;
                }
                @if (item.kategori != "Corba")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a>@item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>

    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYSIZ --- TATLI TARİFLERİ</h2>


        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay == "onay")
                {
                    continue;
                }
                @if (item.kategori != "Tatli")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a>@item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>

    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYSIZ --- İÇECEK TARİFLERİ</h2>
        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay == "onay")
                {
                    continue;
                }
                @if (item.kategori != "Icecek")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a>@item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>

    </div>

</div>
<div class="container" id="onayli" style="display:none;">
    <div style="margin-top:9%">
        <h2 style="text-align:center;">--- ONAYLI --- SON EKLENEN TARİFLER</h2>
        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay != "onay")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İNCELE</a> @item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>
    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYLI --- ANA YEMEK TARİFLERİ</h2>

        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay != "onay")
                {
                    continue;
                }
                @if (item.kategori != "anaYemek")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a> @item.goruntulenme görüntülenme
                    </div>
                </li>

            }
        </ul>

    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYLI --- ÇORBA TARİFLERİ</h2>

        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay != "onay")
                {
                    continue;
                }
                @if (item.kategori != "Corba")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a>@item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>

    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYLI --- TATLI TARİFLERİ</h2>


        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay != "onay")
                {
                    continue;
                }
                @if (item.kategori != "Tatli")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a>@item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>

    </div>
    <div style="margin-top=:11%">
        <h2 style="text-align:center;">--- ONAYLI --- İÇECEK TARİFLERİ</h2>
        <ul class="cards">
            @foreach (var item in Model)
            {
                if (item.tarifOnay != "onay")
                {
                    continue;
                }
                @if (item.kategori != "Icecek")
                {
                    continue;
                }
                <li class="card">
                    <div>
                        <h3 class="card-title">@Html.DisplayFor(modelItem => item.tarifAd)</h3>
                        <img src="~/images/@item.tarifFoto" width="10%" height="10%">
                        <div class="card-content">
                            Yazan Kişi = @item.adSoyad
                            <p>
                                @Html.DisplayFor(modelItem => item.tarifGirisYazisi)
                            </p>
                        </div>
                    </div>
                    <div class="card-link-wrapper">
                        <a class="card-link" asp-action="adminDetails" asp-route-id="@item.tarifID">İncele</a>@item.goruntulenme görüntülenme
                    </div>
                </li>
            }
        </ul>

    </div>

</div>


<script>
    // Get the button
    let mybutton = document.getElementById("onaysizTarifler");
 let yourbutton = document.getElementById("onayliTarifler");
    let onayliTarifler = document.getElementById("onayli");
    let onaysizTarifler = document.getElementById("onaysiz");

    // When the user scrolls down 20px from the top of the document, show the button
    window.onscroll = function () { scrollFunction() };

  
    
    document.getElementById("onaysizTarifler").addEventListener("click", myFunction);

    function myFunction() {
        mybutton.style.display = "none";
        yourbutton.style.display="block";
        onayliTarifler.style.display="none";
        onaysizTarifler.style.display="block";
    };
    document.getElementById("onayliTarifler").addEventListener("click", myFunctionn);

    function myFunctionn() {

        mybutton.style.display = "block";
        yourbutton.style.display = "none";

        onaysizTarifler.style.display = "none";
        onayliTarifler.style.display = "block";

    };
</script>